# To do
# Create final endpoint producer
#    endpoint list + suffix list -> first endpoint suffix pair in suffix[endpoints to call]
# create endpoint specific queues
# Create concurrent endpoint consumer
# Send endpoints to concurrent consumers
#   limiting the number of final endpoints which can be sent to a consumer within endpoint specific limit and period